#include <xc.h>
#include "config.h"

void initSysPins(void);
void initSysDevices(void);

void main(void)
{
    initSysPins();
    initSysDevices();
    
    PORTA = 0b00000001;
    PWM6CONbits.EN = 1;
    while(1)
    {
        //do nothing but loop
    }
            
}
